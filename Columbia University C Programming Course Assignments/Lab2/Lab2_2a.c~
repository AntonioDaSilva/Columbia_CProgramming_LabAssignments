#include <stdio.h>

int main(){
  int h,w;
  printf("Enter the width: ");
  scanf("%d",&w);
  printf("Enter the height: ");
  scanf("%d",&h);
  for(int i = 0; i<w ; i++){
    printf("*");
  }
  printf("\n");
  for (int i = 0; i<(h-2) ; i++){
    printf("*"+(" "*(w-2)+"*"); 
  }
  

}
